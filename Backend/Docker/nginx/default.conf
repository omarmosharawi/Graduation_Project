upstream django {
    server django:8000;
}

# PHASE 1: HTTP & SSL Challenge
server {
    listen 80;
    server_name api.yourdomain.com; # <--- CHANGE THIS

    server_tokens off;

    # This allows Certbot to verify you own the domain
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect all other HTTP traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

# PHASE 2: HTTPS (Currently commented out after certificate exists on your server!)
# server {
#     listen 443 ssl;
#     server_name api.yourdomain.com; # <--- CHANGE THIS
#
#     ssl_certificate /etc/letsencrypt/live/api.yourdomain.com/fullchain.pem; # <--- CHANGE THIS
#     ssl_certificate_key /etc/letsencrypt/live/api.yourdomain.com/privkey.pem; # <--- CHANGE THIS
#
#     location / {
#         proxy_pass http://django;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header Host $host;
#         proxy_redirect off;
#     }
#
#     location /static/ {
#         alias /code/staticfiles/;
#     }
#
#     location /media/ {
#         alias /code/media/;
#     }
# }